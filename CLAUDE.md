# CLAUDE.md

הנחיות לעבודה על הפרויקט הזה עם Claude Code.

## מה זה

דאשבורד Streamlit יחיד (`app.py`) לניתוח נתוני הונאות בכרטיסי אשראי מתוך `credit_card_fraud_10k.csv`. ממשק בעברית (RTL), עיצוב כהה בסגנון "חדר בקרה". אין מודל ML — הניתוח הוא EDA אינטראקטיבי (פילטרים, קורלציות, השוואות התפלגות).

## הרצה

```bash
pip install -r requirements.txt
streamlit run app.py
```

## מבנה

- כל הלוגיקה בקובץ אחד: `app.py`. אין צורך לפצל למודולים בהיקף הנוכחי.
- פלטת הצבעים וקבועי התוויות (`FEATURE_LABELS`, `NUM_FEATURES`, צבעי accent) מוגדרים בראש הקובץ — לשנות עיצוב/תוויות שם, לא במקום אחר.
- ה-CSS ל-RTL ולסגנון הכהה מוזרק פעם אחת דרך `st.markdown(..., unsafe_allow_html=True)` בתחילת הקובץ.
- `.streamlit/config.toml` קובע את ה-theme הבסיסי של Streamlit; שינויי צבע נרחבים יותר נעשים ב-CSS המוזרק, לא שם.

## מוסכמות

- טקסט ממשק (כותרות, תוויות, פילטרים) — בעברית. שמות עמודות/משתנים בקוד — באנגלית, כמקובל.
- גרפים דרך Plotly (`plotly.express`), עם `style_fig()` שמיישם עיצוב אחיד (רקע, גריד, גופן) — כל גרף חדש צריך לעבור דרכה.
- בכל חישוב שמערב חלוקה (שיעורי הונאה, קורלציה) יש לטפל במקרי קצה של מכנה אפס / NaN (למשל סינון שמצמצם לקבוצה ריקה).
